# shared

Place only cross-package assets here.

Use this directory for templates, helper scripts, or supporting files that are reused by more than one playbook or stack package. Keep playbook-specific assets inside the package that owns them.
